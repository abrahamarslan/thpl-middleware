# Development & test requirements (never installed in the production image)
-r requirements.txt

pytest>=8.3
pytest-asyncio>=0.25
pytest-cov>=6.0
pytest-mock>=3.14        # `mocker` fixture — mock external systems (Zoho API, Redis) in service-layer tests with automatic teardown
factory-boy>=3.3
faker>=33.0
ruff>=0.8
mypy>=1.13
pre-commit>=4.0
