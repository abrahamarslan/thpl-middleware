{{ company_name }} password reset code

We received a request to reset your password. Enter this code on the password
reset screen in your app:

    {{ code }}

It is valid for {{ expires_minutes }} minutes{% if expires_at %} (expires at {{ expires_at }}){% endif %}.

Request audit information:
  Account:          {{ email }}
  Request Time:     {{ requested_at }}
  Device:           {{ device }}
  IP & Location:    {{ ip }} ({{ location }})
  Attempts Allowed: {{ attempts_allowed }} attempts maximum before lockout

Do not share this code with anyone. If you didn't request a reset, you can
safely ignore this email. Contact {{ support_email }} if you suspect
unauthorized activity.
